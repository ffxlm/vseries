import SecurityView from '@/components/SecurityView';

export const metadata = {
  title: 'Security Management | SeriesApp Admin',
};

export default function SecurityPage() {
  return <SecurityView />;
}
